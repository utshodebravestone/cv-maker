import Profile from "../Profile";

const CV = () => {
  return (
    <div className="w-full h-full bg-slate-50 rounded shadow-md">
      <Profile />
    </div>
  );
};

// eslint-disable-next-line react-refresh/only-export-components
export default CV;
